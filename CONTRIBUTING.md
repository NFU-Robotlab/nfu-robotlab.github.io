# 網站維護流程

## 一般內容更新

1. 從 `main` 建立新分支。
2. 在 `pages/` 找到對應頁面修改內容。
3. 若只影響單一頁面，樣式與程式分別放到 `assets/css/pages/`、`assets/js/pages/`。
4. 執行 `npm test`。
5. 建立 Pull Request，確認預覽後再合併。

## 檔案命名

- 使用英文小寫與連字號：`laser-cutter-controls.png`。
- 不使用空格、日期截圖名稱、`(1)` 或「未命名」。
- 圖片放在 `assets/images/`，不要放在專案根目錄。

## 不可公開的內容

本儲存庫是公開的。下列內容不得提交：

- 帳號、密碼、API Key 或 Token
- 學生、老師或廠商的個人資料
- 報帳明細、財務資料與未公開計畫資料
- 只供實驗室成員使用的行政操作流程
